import { LocationConnection } from "src/app/common/databases/location/Location.interface";

export const connection: LocationConnection = {
  PATH: "path",
  USERNAME: "username",
  PASSWORD: "password"
};

export default connection;
